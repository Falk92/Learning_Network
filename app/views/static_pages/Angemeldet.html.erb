<!-- <h1>StaticPages#Angemeldet</h1>
<p>Find me in app/views/static_pages/Angemeldet.html.erb</p>
<!--Das hier wird die Seite die man sieht wenn man angemeldet ist-->

<!-- <div class="row">

<% @courses.each do |course| %>
  <div class="col-xs-4 col-md-4">
    <div class="thumbnail">
      <img src="<%= course.image_url %>" alt="<%= course.title %>">
      <div class="caption">
        <h3><%= course.title %></h3>
        <p><%= course.description %></p>
        <h4><%= course.time %></h4>
        <h4><%= course.place %></h4>
        <p><%= link_to 'Show ', course %><%= link_to 'Edit ', edit_course_path(course) %><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></p>
      </div>
      <div class="submit">
        <p>Submit</p>
      </div>
    </div>
  </div>
<% end %> -->

<!-- </div> -->